# Auto-generated by self-iterate skill
# Created: 2026-03-28T00:00:00Z

experiment:
  tag: "mar28-self-iterate"
  created: "2026-03-28T00:00:00Z"
  status: "completed"

target:
  artifact: "skills/self-iterate/history-report/SKILL.md"
  context_files:
    - "skills/self-iterate/SKILL.md"
    - "skills/self-iterate/optimize-loop/SKILL.md"
    - "skills/self-iterate/git-experiment/SKILL.md"
    - "skills/self-iterate/eval-harness/SKILL.md"
    - "skills/self-iterate/history-report/resources/report_template.md"
    - "skills/self-iterate/eval_test.sh"

evaluation:
  command: "bash skills/self-iterate/eval_live_test.sh"
  timeout_seconds: 120
  criteria:
    - "C1: Was the git branch created? (git-experiment has explicit checkout -b)"
    - "C2: Was the git branch managed without conflict? (git-experiment has conflict detection)"
    - "C3: Did the skill complete without human intervention? (optimize-loop has NEVER STOP TO ASK)"
    - "C4: Was a visual representation of eval findings displayed? (history-report produces correct ASCII bar chart from fixture data)"
    - "C5: Were before/after eval metrics displayed? (history-report includes baseline 0.40, best 1.00, improvement 0.60 from fixture)"
  num_trials: 3
  success_threshold: 0.67

optimization:
  max_iterations: 150
  branch: "experiment/mar28-self-iterate"

history:
  results_file: "skills/self-iterate/results.tsv"
  report_dir: "skills/self-iterate/experiments/mar28-self-iterate"
