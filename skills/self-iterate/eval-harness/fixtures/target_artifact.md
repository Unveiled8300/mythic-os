# Fixture Target Artifact

This is a placeholder artifact used by the `eval_live_test.sh` fixture experiment.
It exists so the history-report archive step (`cp <TARGET_ARTIFACT> experiments/<tag>/final_artifact.md`)
has a file to snapshot.

No logic lives here. The real target artifact being optimized is `history-report/SKILL.md`.
